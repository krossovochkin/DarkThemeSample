<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          xmlns:tools="http://schemas.android.com/tools"
          package="com.krossovochkin.darkthemesample">

    <application
            android:name=".App"
            android:allowBackup="false"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/AppTheme.DayNight"
            tools:ignore="GoogleAppIndexingWarning">

        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity android:name=".activity.DefaultActivity"
                  android:theme="@style/AppTheme.Default"/>

        <activity android:name=".activity.DayNightActivity"
                  android:theme="@style/AppTheme.DayNight"/>

        <activity android:name=".activity.ForceDarkActivity"
                  android:theme="@style/AppTheme.Default.ForceDark"/>

        <activity android:name=".activity.NoDarkActivity"
                  android:theme="@style/AppTheme.Default.NoDark"/>
    </application>

</manifest>